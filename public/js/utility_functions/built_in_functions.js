var name = ' Darth Vader  ';

// convert everything to uppercase
name.toUpperCase();

// trim whitespace
name.trim();

// get the number of characters
var length = name.length;

// get a specific character
console.log(name.charAt(1)); // console.logs a

// concat
name.concat(' , that\'s my name bitch'); // concatenates strings

// split character
var split_vader = name.split("h"); // makes ['dart', 'vader']; like explode
