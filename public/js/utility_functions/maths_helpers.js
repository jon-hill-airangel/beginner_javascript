var number = 2.46;
var number2 = 2.67;
console.log(Math.round(number));// logs 2
console.log(Math.round(number2));

var to_be_rounded_ip = 3.45;
cosnsole.log(Math.ceil(to_be_rounded_ip)); // logs 4

// Math.floor(number_to_be_rounded_down);

console.log(Math.max(4, 98, 555)); // logs the highest number in array
console.log(Math.min(5,99, 254)); // returns lowerst number in array

console.log(Math.random()); // returns random number
console.log(Math.floor(Math.random() * 6) + 1); // logs a number between 1 and 6;